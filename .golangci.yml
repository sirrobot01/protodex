run:
  timeout: 5m
  tests: false

issues:
  exclude-dirs:
    - web/
    - migrations/
    - docs/
    - examples/
    - scripts/
    - .github/

  exclude-rules:
    - path: _test\.go
      linters:
        - mnd
        - goconst
        - dupl

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/sirrobot01/protodex
  gocyclo:
    min-complexity: 15
  goconst:
    min-len: 2
    min-occurrences: 2

linters:
  enable:
    - dogsled
    - copyloopvar
    - gofmt
    - goprintffuncname
    - ineffassign
    - misspell
    - nakedret
    - nolintlint
    - staticcheck
    - stylecheck
    - typecheck
    - unconvert
    - unparam
    - unused
    - whitespace